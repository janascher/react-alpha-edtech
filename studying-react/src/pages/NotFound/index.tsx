import { SDiv, SP} from "./styled";

function NotFound() {
    return (
        <SDiv>
            <h2>404</h2>
            <SP>Não encontramos o que você estava procurando.</SP>
        </SDiv>
    );
}

export default NotFound;